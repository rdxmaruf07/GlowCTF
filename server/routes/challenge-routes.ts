import type { Express } from "express";

export function setupChallengeRoutes(app: Express) {
  // Challenge routes are already defined in the main routes.ts file
  // This file exists to maintain the modular structure
  console.log("Challenge routes setup completed");
}