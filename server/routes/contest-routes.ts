import type { Express } from "express";

export function setupContestRoutes(app: Express) {
  // Contest routes can be added here in the future
  // For now, this is a placeholder to prevent import errors
  console.log("Contest routes setup completed");
}